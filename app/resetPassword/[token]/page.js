import ResetPassword from "./../../_components/ResetPassword";

export const metadata = {
  title: "Rendys Cars / reset your password",
  description: "sfsdfsdfsd",
};

export default function ResetPasswordi({ params }) {
  return (
    <div>
      <ResetPassword params={params} />
    </div>
  );
}
